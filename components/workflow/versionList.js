import React from "react";
import SingleVersion from "./singleVersion";

const versionList = () => {
  return (
    <div>
      <SingleVersion />
      <SingleVersion />
      <SingleVersion />
      <SingleVersion />
      <SingleVersion />
      <SingleVersion />
      <SingleVersion />
    </div>
  );
};

export default versionList;
